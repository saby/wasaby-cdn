# Обязательно к прочтению перед обновлением версии React

На данный момент requirejs не умеет работать с анонимными define для этого мы добавляем 
в библиотеки имена, например:
### Было
`define(['exports'], factory)`

### Стало
`define('react', ['exports'], factory)`
